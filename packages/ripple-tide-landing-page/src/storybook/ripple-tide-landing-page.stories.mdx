import {
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs'
import { RplContent } from '@dpc-sdp/ripple-ui-core'
import { default as Layout } from './layout.vue'
import FIXTURE from './fixture.json'

<Meta title='Content types/Landing page' />

# Landing page

<Canvas>
  <Story
    name='Landing page'
    parameters={{
      layout: 'fullscreen'
    }}
  >
    {() => {
      return {
        components: { Layout, RplContent },
        setup() {
          return { FIXTURE }
        },
        template: `<layout :page="FIXTURE"></layout>`
      }
    }}
  </Story>
</Canvas>
