import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplSkipLink from './RplSkipLink.vue'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const FullWidthTemplate = (args) => ({
  components: { RplSkipLink },
  setup() {
    return { args }
  },
  template: `
    <RplSkipLink>Skip to main content</RplSkipLink>
    <p class="rpl-type-p rpl-u-padding-5">Use tab key to see skip link</p>
  `
})

<Meta
  title='Core/Layout/Skip links'
  component={RplSkipLink}
  parameters={{
    layout: 'fullscreen'
  }}
/>

# Layout

<ArgsTable of={RplSkipLink} />

<Canvas>
  <Story
    name='stand alone'
    play={a11yStoryCheck}
  >
    {FullWidthTemplate.bind()}
  </Story>
</Canvas>
